import React from "react";
import { Box } from '@mui/material';
import AppRoutes from './routes';

const App = () => {
  return (
    <Box sx={{ height: '100vh' }}>
      <AppRoutes />
    </Box>
  );
};

export default App;
